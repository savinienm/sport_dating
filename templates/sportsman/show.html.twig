{% extends 'base.html.twig' %}

{% block title %}Sportsman{% endblock %}

{% block body %}
    <h1>Sportsman</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ sportsman.ID }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ sportsman.LastName }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ sportsman.FirstName }}</td>
            </tr>
            <tr>
                <th>County</th>
                <td>{{ sportsman.County }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ sportsman.Email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sportsman_index') }}">back to list</a>

    <a href="{{ path('sportsman_edit', {'ID': sportsman.ID}) }}">edit</a>

    {{ include('sportsman/_delete_form.html.twig') }}
{% endblock %}
