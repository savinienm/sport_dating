<form method="post" action="{{ path('sport_practice_by_sportsman_delete', {'ID': sport_practice_by_sportsman.ID}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sport_practice_by_sportsman.ID) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
